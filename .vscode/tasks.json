{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-all",
      "detail": "Compile generator + vscode + harness before launching the extension host",
      "type": "shell",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": "$tsc",
      "group": "build",
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
