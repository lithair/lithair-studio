{
  "name": "@lithair-studio/harness",
  "version": "0.1.0",
  "private": true,
  "description": "Generative regression-test oracle. `check` (spec -> generate() -> temp Cargo project -> cargo check) is implemented (Layer 2, brick 1); boot -> HTTP CRUD -> event-sourcing invariants are sketched. Shares @lithair-studio/generator with the extension.",
  "license": "MIT OR Apache-2.0",
  "bin": {
    "lithair-harness": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test \"dist/__tests__/*.test.js\""
  },
  "dependencies": {
    "@lithair-studio/generator": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
